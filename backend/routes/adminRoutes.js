const r=require('express').Router(),c=require('../controllers/adminController'),{verifyToken,verifyRole}=require('../middlewares/authMiddleware'); r.use(verifyToken,verifyRole(['admin'])); r.get('/stats',c.getDashboardStats); r.get('/users',c.getAllUsers); r.put('/users/:id/role',c.updateUserRole); r.delete('/users/:id',c.deleteUser); r.get('/documents/pending',c.getPendingDocuments); r.put('/documents/:id/status',c.updateDocumentStatus); module.exports=r;
