import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import { deleteDocument,getAllAdmin } from '../services/documentService';
export default function AdminDocuments(){const [docs,setDocs]=useState([]),[q,setQ]=useState(''),[status,setStatus]=useState('');const load=()=>getAllAdmin().then(setDocs);useEffect(()=>{load()},[]);const del=async id=>{if(!confirm('Xóa vĩnh viễn tài liệu này?'))return;await deleteDocument(id);load()};const f=docs.filter(d=>d.title.toLowerCase().includes(q.toLowerCase())&&(!status||d.status===status));return <main className="section shell"><div className="admin-head"><div><span className="eyebrow">Kho học liệu</span><h1>Quản lý tài liệu</h1></div><div className="filter-row"><input className="input compact" value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm tài liệu..."/><select className="input compact" value={status} onChange={e=>setStatus(e.target.value)}><option value="">Mọi trạng thái</option><option value="pending">pending</option><option value="approved">approved</option><option value="rejected">rejected</option></select></div></div><div className="panel table-wrap"><table><thead><tr><th>Tài liệu</th><th>Người đăng</th><th>Danh mục</th><th>Trạng thái</th><th>Lượt tải</th><th></th></tr></thead><tbody>{f.map(d=><tr key={d.id}><td><Link to={`/documents/${d.id}`}><strong>{d.title}</strong></Link><div className="cell-sub">{d.file_format}</div></td><td>{d.uploader_name||'—'}</td><td>{d.category_name||'—'}</td><td><span className={`badge ${d.status}`}>{d.status}</span></td><td>{d.downloads||0}</td><td><div className="row-actions"><Link to={`/document/edit/${d.id}`}>Sửa</Link><button className="reject" onClick={()=>del(d.id)}>Xóa</button></div></td></tr>)}</tbody></table></div></main>}
