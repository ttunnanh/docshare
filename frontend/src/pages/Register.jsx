import { useState } from 'react'; import { Link,useNavigate } from 'react-router-dom'; import api from '../services/api';
export default function Register(){const [f,setF]=useState({fullname:'',email:'',password:''}),[busy,setBusy]=useState(false),[err,setErr]=useState('');const nav=useNavigate();const submit=async e=>{e.preventDefault();setBusy(true);setErr('');try{await api.post('/auth/register',f);alert('Đăng ký thành công. Hãy đăng nhập.');nav('/login')}catch(e){setErr(e.response?.data?.message||'Đăng ký thất bại.')}finally{setBusy(false)}};return <div className="auth-page"><div className="auth-card"><div className="auth-copy"><span className="eyebrow">Tạo tài khoản</span><h1>Bắt đầu với DocShare</h1><p>Tài khoản mới mặc định là sinh viên để đảm bảo an toàn phân quyền.</p></div>{err&&<div className="alert error">{err}</div>}<form onSubmit={submit} className="form-stack"><label>Họ và tên<input className="input" value={f.fullname} onChange={e=>setF({...f,fullname:e.target.value})} required/></label><label>Email<input className="input" type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})} required/></label><label>Mật khẩu<input className="input" type="password" minLength="6" value={f.password} onChange={e=>setF({...f,password:e.target.value})} required/></label><button disabled={busy} className="btn primary wide">{busy?'Đang tạo...':'Tạo tài khoản'}</button></form><p className="auth-foot">Đã có tài khoản? <Link to="/login">Đăng nhập</Link></p></div></div>}
