import { useEffect,useState } from 'react'; import { getPendingDocuments,setDocumentStatus } from '../services/adminService';
export default function AdminApproval(){const [docs,setDocs]=useState([]),[busy,setBusy]=useState(true);const load=()=>getPendingDocuments().then(setDocs).finally(()=>setBusy(false));useEffect(load,[]);const act=async(id,status)=>{if(!confirm(status==='approved'?'Duyệt tài liệu này?':'Từ chối tài liệu này?'))return;await setDocumentStatus(id,status);load()};return <main className="section shell"><div className="page-title left"><span className="eyebrow">Kiểm duyệt</span><h1>Tài liệu chờ duyệt</h1><p>Chỉ tài liệu được duyệt mới xuất hiện ở trang khám phá.</p></div>{busy?<div className="state-card">Đang tải...</div>:!docs.length?<div className="state-card">Không có tài liệu nào đang chờ duyệt.</div>:<div className="panel table-wrap"><table><thead><tr><th>Tài liệu</th><th>Người đăng</th><th>Danh mục</th><th>Ngày gửi</th><th>Thao tác</th></tr></thead><tbody>{docs.map(d=><tr key={d.id}><td><strong>{d.title}</strong><div className="cell-sub">{d.file_format}</div></td><td>{d.uploader_name||'Ẩn danh'}</td><td>{d.category_name||'—'}</td><td>{new Date(d.created_at).toLocaleDateString('vi-VN')}</td><td><div className="row-actions"><button className="approve" onClick={()=>act(d.id,'approved')}>Duyệt</button><button className="reject" onClick={()=>act(d.id,'rejected')}>Từ chối</button></div></td></tr>)}</tbody></table></div>}</main>}
