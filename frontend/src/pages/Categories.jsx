import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import { FiFolder } from 'react-icons/fi'; import { getCategories } from '../services/categoryService';
export default function Categories(){const [cats,setCats]=useState([]);useEffect(()=>{getCategories().then(setCats)},[]);return <main className="section shell"><div className="page-title"><span className="eyebrow">Phân loại học liệu</span><h1>Danh mục</h1><p>Chọn một lĩnh vực để khám phá tài liệu đã được duyệt.</p></div><div className="category-grid">{cats.map(c=><Link key={c.id} to={`/?category_id=${c.id}`} className="category-card"><span><FiFolder/></span><h3>{c.name}</h3><p>{c.description||'Xem các tài liệu thuộc danh mục này.'}</p></Link>)}</div></main>}
