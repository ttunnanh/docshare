import { useEffect,useState } from 'react'; import { createCategory,deleteCategory,getCategories,updateCategory } from '../services/categoryService';
export default function AdminCategories(){const [cats,setCats]=useState([]),[name,setName]=useState(''),[edit,setEdit]=useState(null);const load=()=>getCategories().then(setCats);useEffect(()=>{load()},[]);const submit=async e=>{e.preventDefault();try{edit?await updateCategory(edit,name):await createCategory(name);setName('');setEdit(null);load()}catch(e){alert(e.response?.data?.message||'Không thể lưu danh mục.')}};const del=async id=>{if(!confirm('Xóa danh mục này?'))return;try{await deleteCategory(id);load()}catch(e){alert(e.response?.data?.message||'Không thể xóa.')}};return <main className="section shell"><div className="page-title left"><span className="eyebrow">Cấu trúc học liệu</span><h1>Danh mục</h1></div><form className="panel inline-form" onSubmit={submit}><input className="input" placeholder="Tên danh mục" value={name} onChange={e=>setName(e.target.value)} required/><button className="btn primary">{edit?'Cập nhật':'Thêm danh mục'}</button>{edit&&<button className="btn ghost" type="button" onClick={()=>{setEdit(null);setName('')}}>Hủy</button>}</form><div className="panel table-wrap"><table><thead><tr><th>ID</th><th>Tên danh mục</th><th></th></tr></thead><tbody>{cats.map(c=><tr key={c.id}><td>{c.id}</td><td><strong>{c.name}</strong></td><td><div className="row-actions"><button onClick={()=>{setEdit(c.id);setName(c.name)}}>Sửa</button><button className="reject" onClick={()=>del(c.id)}>Xóa</button></div></td></tr>)}</tbody></table></div></main>}
