import { Link } from 'react-router-dom';
import { FiDownload,FiBookmark,FiBookOpen } from 'react-icons/fi';
export default function DocumentCard({doc,saved=false,onSave,onDownload}){return <article className="doc-card"><div className="doc-top"><span className="format-chip">{doc.file_format||'FILE'}</span>{onSave&&<button className={saved?'save-btn saved':'save-btn'} onClick={()=>onSave(doc.id)} title="Lưu"><FiBookmark/></button>}</div><Link className="doc-title" to={`/documents/${doc.id}`}>{doc.title}</Link><p className="doc-desc">{doc.description||'Chưa có mô tả cho tài liệu này.'}</p><div className="doc-meta"><span><FiBookOpen/> {doc.category_name||'Chưa phân loại'}</span><span>{doc.uploader_name||'Ẩn danh'}</span></div><div className="doc-footer"><span>{doc.downloads||0} lượt tải</span>{onDownload&&<button className="btn ghost small" onClick={()=>onDownload(doc.id)}><FiDownload/> Tải xuống</button>}</div></article>}
